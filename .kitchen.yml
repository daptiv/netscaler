---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[netscaler::test_server]
      - recipe[netscaler::test_servicegroup]
      - recipe[netscaler::test_binding]
      - recipe[netscaler::test_serverup]
    attributes:
